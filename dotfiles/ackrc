# -*- mode: conf-unix; -*-

# Add some more types
--type-set
gsp=.gsp
--type-set
scss=.scss
--type-set
sass=.sass

# Always color, even if piping to another program.
# To turn off color, use the `--nocolor' argument.
--color

# Ignore `.tox' directories.
--ignore-dir=.tox

# Almost always want to ignore `build' directories.
# To unignore, use `--noignore-dir=build'.
--ignore-dir=build
