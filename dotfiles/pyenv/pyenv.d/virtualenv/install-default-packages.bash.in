# It'd be nice to use --requirement here, but it doesn't support
# stdin, and is probably not worth the trouble.
after_virtualenv 'PYENV_VERSION="${VIRTUALENV_NAME}" pyenv-exec pip install $QUIET $VERBOSE @DEFAULT_PACKAGES@'
