#!/usr/bin/env bash

set -o nounset
set -o errexit

if [[ $# -ne 1 || $1 == -h || $1 == --help ]]; then
	echo "Usage: $0 PYTHON_VERSION" >&2
	exit 1
fi

readonly PYTHON_VERSION=$1
readonly VENV=${PWD##*/}${PYTHON_VERSION//./}

@PYENV@ virtualenv "$PYTHON_VERSION" "$VENV"
@PYENV@ local "$VENV"
